# =============================================================================
# Environment Variables - Example
# =============================================================================
# Copy this file to .env.local and fill in the values.

# --- Debugging ---

# Enable detailed console logging for the Sales Notes creation/edit flow.
# Set to "true" to see [SALES_NOTE_FLOW] logs in the browser console and server logs.
# Default: disabled (any value other than "true" or unset).
NEXT_PUBLIC_DEBUG_SALES_FLOW=false
